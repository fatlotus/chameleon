package main

import (
	"image"
	_ "image/png"
	"flag"
)


var outpath = flag.String("out", nil, "where to write the charts")
var pcap = flag.String("pcap", nil, "where to read data from")

func main() {
	
}